name: Publish NuGet

on:
  push:
    branches: 
    - main

jobs:
  publish-nuget:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: RacoonSecure/RacoonSecure.Core
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '3.1.x'
    
    - name: Install dependencies
      run: dotnet restore
    
    - name: Build
      run: dotnet build --configuration Release --no-restore

    - name: Run Tests
      uses: zyborg/dotnet-tests-report@v1
      with:
        project_path: RacoonSecure.Core.Tests
        report_name: RacoonSecureTests
        report_title: RacoonSecure Tests
        github_token: ${{ secrets.GITHUB_TOKEN }}
      


    # - name: Publish to NuGet
    #   uses: brandedoutcast/publish-nuget@v2
    #   with:
    #     PROJECT_FILE_PATH: RacoonSecure/RacoonSecure.Core/RacoonSecure.Core.csproj
    #     VERSION_REGEX: '^\s*<PackageVersion>(.*)<\/PackageVersion>\s*$'
    #     TAG_FORMAT: '*'
    #     NUGET_KEY: ${{secrets.NUGET_API_KEY}}